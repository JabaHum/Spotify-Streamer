<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin">

    <LinearLayout
        android:id="@+id/listItemThumbnail"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_alignParentLeft="true"
        android:layout_marginEnd="5dp"
        android:layout_marginRight="5dp"
        android:padding="3dp">
        <ImageView
            android:id="@+id/listItemArtistImageView"
            android:layout_width="?android:listPreferredItemHeightLarge"
            android:layout_height="?android:listPreferredItemHeightLarge"
            android:src="@mipmap/ic_launcher"
            android:contentDescription="@string/app_name" />
    </LinearLayout>

    <TextView xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/listItemArtistTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_toEndOf="@+id/listItemThumbnail"
        android:layout_toRightOf="@+id/listItemThumbnail"
        android:layout_marginStart="7dp"
        android:layout_marginLeft="7dp"
        android:minHeight="?android:listPreferredItemHeightLarge"
        android:gravity="center_vertical"
        android:textSize="16sp"
        android:text="@string/app_name" />

</RelativeLayout>